# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION (Prisma + Supabase/PostgreSQL)
# -----------------------------------------------------------------------------
# URL de conexão usada pelo Prisma para queries normais (Pooling)
DATABASE_URL="postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres?pgbouncer=true"

# URL de conexão direta (usada para Migrations do Prisma)
DIRECT_URL="postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres"

# Senha do banco de dados para referência manual
DATABASE_PASSWORD=sua_senha_aqui

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Storage / Auth se houver)
# -----------------------------------------------------------------------------
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_KEY=sua_anon_key_ou_service_role_key

# -----------------------------------------------------------------------------
# AUTHENTICATION (JWT & Security)
# -----------------------------------------------------------------------------
# Chave mestra para assinar os tokens (mínimo 32 caracteres recomendados)
JWT_SECRET=sua_chave_secreta_super_segura_aqui_min_32_caracteres

# Tempo de vida dos tokens
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Custo do hash de senha (quanto maior, mais seguro e mais lento)
BCRYPT_SALT_ROUNDS=10

# -----------------------------------------------------------------------------
# IA (Gemini API)
# -----------------------------------------------------------------------------
# Chave de API obtida no Google AI Studio
GEMINI_API_KEY=sua_api_key_do_gemini_aqui

# -----------------------------------------------------------------------------
# APP SETTINGS
# -----------------------------------------------------------------------------
PORT=3000
NODE_ENV=development